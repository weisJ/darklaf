name: Autostyle

on: [push, pull_request]

jobs:
  autostlye:
    name: "Errorprone"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 10
      - name: Check
        run: ./gradlew compileJava -PenableErrorProne
